// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Custom domain on GitHub Pages (root): https://imoblisthub.com.br/
  site: 'https://imoblisthub.com.br',
  base: '/',
});
