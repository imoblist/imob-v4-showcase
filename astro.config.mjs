// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // GitHub Pages (project site): https://imoblist.github.io/imob-v4-showcase/
  // When you later move to a custom root domain, switch `site` and `base` back to '/'.
  site: 'https://imoblist.github.io',
  base: '/imob-v4-showcase/',
});
