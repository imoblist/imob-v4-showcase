{
  "name": "imob-v4-showcase",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf docs",
    "build": "npm run clean && mkdir -p docs && cp index.html styles.css scripts.js CNAME docs/"
  }
}
